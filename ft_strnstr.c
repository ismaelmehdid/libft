#include <stdio.h>
#include <string.h>

char	*ft_strnstr(const char *big, const char *little, size_t len)
{
	char *array;
	return (array);
}

int main(void)
{
	char array[] = "je suis ismamel";

	printf("%s", strnstr(array, "suis", 15));
}